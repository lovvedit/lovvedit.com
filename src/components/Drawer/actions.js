import * as types from './types';

export const toggle = () => ({
  type: types.TOGGLE,
});

export const open = () => ({
  type: types.OPEN,
});

export const close = () => ({
  type: types.CLOSE,
});
